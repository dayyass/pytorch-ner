repos:
- repo: local
  hooks:
    - id: unittest
      name: unittest
      entry: python -m unittest discover
      language: system
      description: Run all unittests.
      always_run: true
      types: [python]
      pass_filenames: false
      stages: [commit]